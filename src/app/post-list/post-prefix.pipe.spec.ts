import { PostPrefixPipe } from './post-prefix.pipe';

describe('PostPrefixPipe', () => {
  it('create an instance', () => {
    const pipe = new PostPrefixPipe();
    expect(pipe).toBeTruthy();
  });
});
